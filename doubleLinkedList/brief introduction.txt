基于单向链表的缺点，查找方向只能是一个方向，且不能自我删除，每次都需要一个辅助指针

1 遍历方式和单链表一样，只是可以向前，也可以向后查找
2 添加（默认添加到最后）：
	（1）先遍历，找到双向链表的尾部temp
	（2）temp.next指向新的节点
	（3）new.pre = temp
3 修改思路和原理,和单项一样
4 删除
	（1）因为是双向链表，因此，我们可以实现自我删除，找到X
	（2）X.pre.next = X.next;
	（3）X.next.pre = X.pre;

